export enum UserRole {
    ADMIN = "admin",
    STUDENT = "student",
    PARENT = "parent",
    COACH = "coach",
    HCOACH = "hcoach",
}
export enum Gender {
    MALE = "male",
    FEMALE = "female",
}